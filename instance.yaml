default_context:
  initial_user_name: "admin"
  initial_user_password: "admin"

  # wsgi_listen: localhost:8082

  zcml_package_includes: ploneconf.site, Products.PDBDebugMode, plone.reload
  # if you want to include the training.votable:
  # zcml_package_includes: ploneconf.site, Products.PDBDebugMode, plone.reload, training.votable

  # include meta.zcml of collective.monkeypatcher to provide directive monkey:patch for Products.PDBDebugMode
  zcml_package_metas: collective.monkeypatcher

  db_storage: direct
